# 2026-05-09


## [21:40] Pre-compact flush (unknown)

### Decisions
- Drop `--bare` from heartbeat LLM calls, use `--setting-sources user --disable-slash-commands` + OAuth token injection to keep Max plan billing without hook overhead
- Phase 8 security audit before Phase 9 scheduling ensures unattended tasks ship hardened
- Hook command scanning scopes to Bash/PowerShell tool actions only, not Edit/Write content (prevents false-positives on legitimate deletion discussions)

### Lessons
- `--bare` deliberately skips OAuth and only reads `ANTHROPIC_API_KEY` (fails with 401 on OAuth tokens); this is intentional security, not a bug
- Pre-tool-use hooks scanning all tool_input inadvertently block legitimate edits (discovered when hook blocked Write with "rm" in test content); scope to actions, not data
- SessionEnd hook fires but logs empty transcripts — investigate transcript_path payload and file timing before scheduling

### Facts
- OAuth token injected as `CLAUDE_CODE_OAUTH_TOKEN` in subprocess env, `.claude/data/claude-token.txt` stays gitignored by wildcard + allowlist
- Discord bot verified: 2 DM round-trips (8-14s each), correct vault citations, lowercase voice match
- Phase 8 security: 19/19 guardrail tests pass; sanitize wraps external content with source tags; 4 injection patterns + 7 destructive command patterns detected
- All token references are variable names in Python wrappers, no literals in prompts/skills

### Open todos
- Fix SessionEnd flush bug (empty transcripts) so Phase 6-8 work captures to Obsidian before Phase 9 ships
- Phase 9: Wire Windows Task Scheduler for heartbeat (30 min), indexer (10 min), Discord bot (logon)


## [Backfilled 2026-05-11 from file mtimes] Phase 8 file timeline

The 21:40 flush block above is the source of truth for what was decided. The
file mtimes below just record the order things landed for posterity.

- 21:10 — `.claude/data/state/nudges.json` (heartbeat habit-nudge bookkeeping)
- 21:25-21:32 — `.claude/scripts/security/` — `guardrails.py`, `sanitize.py`, `_smoke_test.py`, plus settings.json registering the new hooks
- 21:26 — `.claude/hooks/pre-tool-use-guard.py` (the dangerous-command blocker)
- 21:27 — `.claude/chat/handler.py` (Phase 7 DM chat reply handler)
- 21:43 — `.claude/hooks/session-end-flush.py` and `pre-compact-flush.py` (context persistence hooks)
- 21:45 — `.claude/scripts/heartbeat/llm.py` (the `claude -p` subprocess wrapper, switched off `--bare`)
- 21:46 — `.claude/hooks/_lib.py` (shared hook utilities)

<!-- timeline:begin -->
## Timeline
← [[2026-05-08]] · [[2026-05-10]] →
<!-- timeline:end -->
