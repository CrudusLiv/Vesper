# Runtime deps for the worker image (discord-bot + scheduler).
# Curated subset of .claude/requirements.txt — omits the host/GUI deps
# (winotify, pystray, customtkinter, pyinstaller, Pillow): winotify is
# Windows-only and toasts no-op in the container; the tray GUI never runs here.
anthropic>=0.40.0
discord.py>=2.3
fastembed>=0.4.0
sqlite-vec>=0.1.6
PyGithub>=2.1
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
python-pptx>=0.6
pypdf>=4.0
schedule>=1.2
Pillow>=10.0
pdf2image>=1.16
easyocr>=1.7
